import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    background: {
      default: '#F3EFD4',
    },
    primary: {
      main: '#F5A141',
    },
  },
});

export default theme;
